<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".AuraApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_aura_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_aura_logo"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Aura">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Aura.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LockActivity" android:theme="@style/Theme.Aura.Splash" />

        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.Aura" />

        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Aura"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.incidents.IncidentDetailActivity"
            android:theme="@style/Theme.Aura.Detail"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.accesses.AccessDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail" />

        <activity
            android:name=".ui.accesses.AccessLogDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail" />

        <activity
            android:name=".ui.services.ContractedServiceDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail" />

        <activity
            android:name=".ui.community.CommunityPostDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.documents.DocumentDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail" />

        <activity
            android:name=".ui.payments.PaymentSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.Aura.Detail" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <service
            android:name=".fcm.AuraFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".ui.fcm.QuickActionActivity"
            android:exported="true"
            android:theme="@style/Theme.Aura">
            <intent-filter>
                <action android:name="QUICK_ACTION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>
</manifest>